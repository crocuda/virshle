# INFO (deprecated)

These functions are not currently used through the flake.

They initially were used to build disk image (fast) but has been replaced
in favour of disko (slow) but easy to parameterize.
